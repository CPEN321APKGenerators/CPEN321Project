version: "3.1"
recipe: default.v1
language: en
assistant_id: 20250310-123456-custom-gpt-assistant

pipeline:
  - name: SingleStepLLMCommandGenerator
    llm:
      model_group: openai_direct
    flow_retrieval:
      embeddings:
        model_group: openai_embeddings
    user_input:
      max_characters: 420  

policies:
  - name: rasa.core.policies.flow_policy.FlowPolicy
